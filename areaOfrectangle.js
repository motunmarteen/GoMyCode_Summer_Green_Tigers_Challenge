// Arithmetic operators: Write JavaScript code to calculate the area of a rectangle given its length and width.

let lengths = Number(prompt("what is the length of the rectangle? : "))
let widths = Number(prompt("what is the width of the rectangle? : "))

let areaOfRectangle = lengths * widths

console.log(areaOfRectangle)